# SAT-SA — core analytics engine.
# Everything the deterministic pipeline (main.py) needs, and nothing
# that assumes a GUI. Install this alone to run assessments headless.
pandas
pyyaml
reportlab
requests
